[tox]
envlist = py27

[testenv]
commands = pytest --cov-fail-under=100 -vv
deps = 
    service_identity 
    mock
    pytest
    pytest-cov
    pytest-flakes
    pytest-twisted
setenv = 
    HOME = {toxinidir}
